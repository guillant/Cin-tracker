{
  "name": "cinetracker",
  "version": "1.0.0",
  "description": "Application mobile et PWA pour suivre films et series.",
  "private": true,
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "test": "node --test",
    "mobile:prepare": "node scripts/prepare-mobile.js",
    "cap:add:android": "cap add android",
    "cap:add:ios": "cap add ios",
    "cap:sync": "npm run mobile:prepare && cap sync",
    "cap:open:android": "cap open android",
    "cap:open:ios": "cap open ios",
    "cap:run:android": "cap run android",
    "cap:run:ios": "cap run ios",
    "android:build:debug": "npm run cap:sync && android\\gradlew.bat -p android assembleDebug",
    "check:js": "node --check js/app.js && node --check js/config.js && node --check service-worker.js && node --check server.js"
  },
  "devDependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1",
    "@capacitor/ios": "^8.3.1"
  }
}
